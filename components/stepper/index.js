import Stepper from "antd-mobile/lib/stepper/index";
import React from 'react';
import ReactDOM from 'react-dom';

const JwStepper= (props) => (
    <Stepper className="jw-stepper" {...props} />
);

export default JwStepper;
