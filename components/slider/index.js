import Slider from "antd-mobile/lib/slider/index";
import React from 'react';
import ReactDOM from 'react-dom';

const JwSlider= (props) => (
    <Slider className="jw-slider" {...props} />
);

export default JwSlider;
