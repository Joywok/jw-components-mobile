import Rate from "antd/lib/rate/index";
import React from 'react';
import ReactDOM from 'react-dom';

const JwRate= (props) => (
    <Rate className="jw-rate" {...props} />
);

export default JwRate;
